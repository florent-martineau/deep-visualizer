name: API

on:
  pull_request:

jobs:
  checks:
    defaults:
      run:
        working-directory: api
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Get UV version from devbox
        id: uv-version
        run: |
          UV_VERSION=$(jq -r '.packages[] | select(startswith("uv@")) | sub("uv@"; "")' devbox.json)
          echo "version=$UV_VERSION" >> $GITHUB_OUTPUT
        working-directory: ./api
        
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
          uv-version: ${{ steps.uv-version.outputs.version }}
          
      - name: Check dependencies
        run: devbox run -- uv run ./scripts/check_dependencies.py
      - name: Static type checking
        run: devbox run -- uv run mypy .

  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
